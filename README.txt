A complete Web Application featuring User Roles (Coach/User & Admin), Workouts, Forum functionality and Calculator.

Data is fetched from SQL DB, API is used to fetch data about total users, workouts and forum posts. 

The application is created with ASP.NET in a Model-View-Controller style, utilizing C# as the back-end language and HTML5 & CSS3 as the front-end. 
